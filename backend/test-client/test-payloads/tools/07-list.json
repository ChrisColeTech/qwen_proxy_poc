{
  "model": "qwen3-max",
  "messages": [
    {
      "role": "system",
      "content": "You are a helpful AI assistant with access to tools. When the user asks for a tree view of directories, use the list tool."
    },
    {
      "role": "user",
      "content": "Show me a tree view of the src directory structure"
    }
  ],
  "tools": [
    {
      "type": "function",
      "function": {
        "name": "list",
        "description": "Shows a formatted tree view of directory contents with automatic filtering of build/dependency folders",
        "parameters": {
          "type": "object",
          "properties": {
            "path": {
              "type": "string",
              "description": "Directory path to list"
            },
            "max_depth": {
              "type": "number",
              "description": "Maximum depth to traverse"
            }
          },
          "required": ["path"]
        }
      }
    }
  ],
  "temperature": 0.7,
  "max_tokens": 2000
}
