# Qwen Proxy Configuration
PROXY_BASE_URL=http://localhost:3000
PROXY_API_KEY=optional-api-key-if-needed

# Qwen API Configuration (for direct comparison)
QWEN_API_KEY=your-qwen-api-key-here
QWEN_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# Test Configuration
TEST_MODE=proxy
# Options: proxy, direct
# proxy: Test against our Qwen proxy
# direct: Test against Qwen API directly (for comparison)

# Working Directory (for tool execution)
WORKING_DIR=/tmp/test-client

# Logging
LOG_LEVEL=info
# Options: debug, info, warn, error
