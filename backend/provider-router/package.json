{
  "name": "qwen-provider-router",
  "version": "1.0.0",
  "description": "Provider abstraction router for testing multiple LLM backends",
  "type": "module",
  "main": "src/index.js",
  "bin": {
    "provider-cli": "./bin/provider-cli.js"
  },
  "scripts": {
    "start": "npx kill-port 3001 && node src/index.js",
    "dev": "npx kill-port 3001 && node --watch src/index.js",
    "test": "node --test tests/**/*.test.js"
  },
  "keywords": [
    "llm",
    "router",
    "provider",
    "qwen",
    "lm-studio"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.6.0",
    "better-sqlite3": "^9.6.0",
    "commander": "^11.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
